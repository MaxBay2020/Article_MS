import {render, screen} from '@testing-library/react'
import Wrapper from "../../testing/Wrapper";
import ArticlePage from "./ArticlePage";


describe('Should render 3 articles with MOCK HTTP request', () => {
    // it('Should render article table', async () => {
    //     render(<Wrapper><ArticlePage /></Wrapper>)
    //     const articleRows = await screen.getAllByTestId('articleRow')
    //     expect(articleRows.length).toBe(3)
    // })
})
